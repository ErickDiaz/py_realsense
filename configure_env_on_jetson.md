# Configure Virtual Env on Jetson Nano

Python 3.6.9

#### Create  virtual env:
`mkvirtualenv realsense -p python3`

#### Activate virtual env:
`workon realsense

### Intall libraries
`pip install numpy`
``
